# import math

# # Define range
# start = int(input())
# end = int(input())

# # Compute factorial using lambda and map
# factorial_list = list(map(lambda x: math.factorial(x), range(start, end + 1)))

# # Print result
# print(f"Factorial of numbers from {start} to {end}: {factorial_list}")


        